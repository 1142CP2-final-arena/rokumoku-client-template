services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${BOT_IMAGE:-rokumoku-bot-test}
    env_file:
      - .env
    environment:
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"
    init: true
    restart: "no"
